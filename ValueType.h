#include <string>

enum ValueType {
    IntNum,
    LongNum,
    CharNum, 
    FloatNum,
    DoubleNum,
    PtrVal,
};

std::string ValueTypeNames[] = {"Int", "Long", "Char", "Float", "Double", "Pointer"};